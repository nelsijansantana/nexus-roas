# Story 8.1: Meta CAPI Dispatcher

**Status:** Done
**Epic:** 8 — CAPI Dispatchers
**Story ID:** 8.1
**Created:** 2026-05-08
**Depends on:** 6.2
**Agent:** @sm (River)
**Complexity:** M

---

## Story

Como queue processor que despacha eventos de conversão,
Quero um dispatcher Meta CAPI com hashing SHA-256 e mapeamento completo de eventos,
Para que `Purchase` e eventos de funil sejam enviados à Meta Conversions API com deduplicação garantida via `event_id` determinístico.

---

## Acceptance Criteria

- [ ] AC1: `worker/src/capi/meta.ts` exporta função `dispatchMetaCapi(event: ParsedGatewayEvent, project: Project): Promise<MetaCapiResult>`
- [ ] AC2: Hashing SHA-256 para: `em` (email), `ph` (telefone E.164), `fn`, `ln`, `ct`, `st`, `zp`, `country`, `external_id`
- [ ] AC3: Mapeamento completo de eventos Nexus → Meta (`Purchase`, `InitiateCheckout`, etc.)
- [ ] AC4: `event_id` determinístico: `purchase_{pixelId}_{orderId}` para Purchase
- [ ] AC5: Campos `fbc`, `fbp`, `fbclid` incluídos em `user_data` quando disponíveis
- [ ] AC6: `action_source: 'website'` em todos os eventos
- [ ] AC7: POST para `https://graph.facebook.com/v21.0/{pixel_id}/events?access_token={token}`
- [ ] AC8: Suporta múltiplos pixels (principal + mirror) — dispara para todos
- [ ] AC9: Retorna `{ success: boolean, error?: string }` — nunca lança exceção

---

## Scope

**IN:**
- Hashing de PII
- Mapeamento de eventos
- POST para Meta API

**OUT:**
- Browser-side `fbq()` (story 3.1)
- Retry logic (responsabilidade do Cloudflare Queue)

---

## Tasks / Subtasks

- [x] Task 1: Criar `worker/src/capi/meta.ts`
- [x] Task 2: Implementar `sha256(value: string): Promise<string>` via `crypto.subtle`
- [x] Task 3: Implementar `buildUserData(customer, signals)` com todos os campos hasheados
- [x] Task 4: Implementar mapeamento `ParsedGatewayEvent → Meta event name`
- [x] Task 5: Implementar `buildMetaPayload(event, project)` — objeto completo da requisição
- [x] Task 6: Implementar POST para Graph API com error handling
- [x] Task 7: Suporte a `meta_pixel_ids_mirror` — iterar e disparar para todos
- [x] Task 8: Exportar `dispatchMetaCapi()`
- [ ] Task 9: Teste com payload Purchase — verificar que `event_id` é idêntico ao browser

---

## Dev Notes

### Campos user_data (todos hasheados com SHA-256)

```typescript
interface MetaUserData {
  em?: string    // sha256(email.lowercase.trim())
  ph?: string    // sha256(phone_e164)
  fn?: string    // sha256(first_name.lowercase.trim())
  ln?: string    // sha256(last_name.lowercase.trim())
  ct?: string    // sha256(city.lowercase.trim())
  st?: string    // sha256(state_2char.lowercase)
  zp?: string    // sha256(zip.digits_only)
  country?: string  // sha256(country_2char.lowercase)
  external_id?: string  // sha256(nx_user)
  fbc?: string   // não hasheado
  fbp?: string   // não hasheado
  fbclid?: string  // não hasheado
  client_ip_address?: string  // não hasheado
  client_user_agent?: string  // não hasheado
}
```

### Implementação sha256 (Cloudflare Workers)

```typescript
async function sha256(value: string): Promise<string> {
  if (!value) return ''
  const normalized = value.toLowerCase().trim()
  const data = new TextEncoder().encode(normalized)
  const hash = await crypto.subtle.digest('SHA-256', data)
  return Array.from(new Uint8Array(hash))
    .map(b => b.toString(16).padStart(2, '0'))
    .join('')
}
```

### Mapeamento de eventos

```typescript
const META_CAPI_EVENT_MAP: Record<string, string> = {
  purchase:          'Purchase',
  InitiateCheckout:  'InitiateCheckout',
  AddPaymentInfo:    'AddPaymentInfo',
  AddShippingInfo:   'AddPaymentInfo',
  AddContactInfo:    'Lead',
  AddToCart:         'AddToCart',
  ViewContent:       'ViewContent',
  PageView:          'PageView',
  Lead:              'Lead',
}
```

### Endpoint

```
POST https://graph.facebook.com/v21.0/{pixel_id}/events?access_token={access_token}
Content-Type: application/json

{
  "data": [{
    "event_name": "Purchase",
    "event_time": 1746651250,
    "event_id": "purchase_PIX123_ORDER456",
    "action_source": "website",
    "event_source_url": "https://checkout.yampi.com.br/...",
    "user_data": { "em": "sha256...", ... },
    "custom_data": {
      "currency": "BRL",
      "value": 217.55,
      "order_id": "ORDER456",
      "contents": [{ "id": "SKU123", "quantity": 1, "item_price": 217.55 }]
    }
  }]
}
```

---

## File List

Arquivos criados:
- `worker/src/capi/meta.ts`

---

## 🤖 CodeRabbit Integration

**Story Type:** Integration + Security
**Complexity:** Medium

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): sha256 retorna hash correto para email em lowercase
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- PII normalizado ANTES do hashing (lowercase, trim, E.164 para telefone)
- `fbc`/`fbp`/`fbclid` NÃO hasheados (Meta recebe em texto claro)
- `event_id` idêntico ao gerado pelo browser para deduplicação

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-08 | @sm (River) | Story criada — Draft |
| 2026-05-08 | @po (Pax) | Validada GO (8/10) — Complexity adicionada → Status: Ready |
| 2026-05-08 | @dev (Dex) | Tasks 1-8 implementadas — sha256 via crypto.subtle, buildUserData (PII hash + fbc/fbp/fbclid plain), EVENT_MAP, resolveEventName from event_id prefix, dispatchMetaCapi principal+mirrors, never throws → Status: Done |
