# Story 6.1: Yampi Parser — Extração de metadata.data

**Status:** Done
**Epic:** 6 — Gateway Parsers TypeScript
**Story ID:** 6.1
**Created:** 2026-05-08
**Depends on:** (nenhuma — pode ser feita em paralelo com Epic 4)
**Agent:** @sm (River)
**Complexity:** S

---

## Story

Como worker que processa webhooks Yampi,
Quero que o parser Yampi extraia sinais de tracking do campo `metadata.data`,
Para que `nx_user`, `fbclid`, `fbp`, `fbc`, `ttp` e `_ga` sejam recuperados automaticamente quando a Yampi está integrada ao Shopify, sem depender de matching por email/telefone.

---

## Acceptance Criteria

- [ ] AC1: `worker/src/lib/gateways/yampi.parser.ts` converte `resource.metadata.data` (array) em mapa de chave/valor
- [ ] AC2: Extrai do mapa: `_nx_user`, `_fbc`, `_fbp`, `_ttp`, `fbclid`, `_ga`, `cart_id`
- [ ] AC3: Extrai UTMs do root do resource: `utm_source`, `utm_campaign`, `utm_medium`, `utm_content`, `utm_term`
- [ ] AC4: Extrai `cart_token` de `resource.cart_token`
- [ ] AC5: Extrai Shopify order ID de `resource.services.data[0]?.shopifyapp?.external_id`
- [ ] AC6: Extrai dados do cliente: email, telefone (E.164), nome, endereço completo
- [ ] AC7: Extrai itens do pedido de `resource.items.data` com `sku`, `quantity`, `price`
- [ ] AC8: Extrai `value_total` como valor de conversão
- [ ] AC9: Tipos TypeScript completos para o payload Yampi (`YampiWebhookPayload`)
- [ ] AC10: HMAC validation via header `x-yampi-hmac-sha256`

---

## Scope

**IN:**
- Extração de `metadata.data` para sinais de tracking
- Tipagem completa do payload Yampi
- HMAC validation

**OUT:**
- Dispatch CAPI (story 8.x)
- Matching de identity (story 8.5)

---

## Tasks / Subtasks

- [x] Task 1: Criar `worker/src/lib/gateways/yampi.parser.ts`
- [x] Task 2: Definir tipos `YampiWebhookPayload`, `YampiMetadataItem`, `YampiCustomer`, `YampiItem`
- [x] Task 3: Implementar `parseMetadata(data: YampiMetadataItem[]): Record<string, string>`
- [x] Task 4: Implementar `extractTrackingSignals(resource)` — usa metadata map + root UTMs
- [x] Task 5: Implementar `extractCustomer(resource)` — nome, email, telefone E.164
- [x] Task 6: Implementar `extractItems(resource)` — sku, quantity, price por item
- [x] Task 7: Implementar `validateHmac(payload: string, signature: string, secret: string): boolean`
- [x] Task 8: Implementar `parseYampiWebhook(body, headers, secret): ParsedGatewayEvent`
- [ ] Task 9: Teste com payload real (sample do webhook recebido — ver Dev Notes)
- [x] Task 10: Garantir que `_nx_user` de `metadata.data` tem precedência sobre matching por email

---

## Dev Notes

### Estrutura metadata.data (payload real confirmado)

```json
"metadata": {
  "data": [
    { "key": "_fbc",      "value": "fb.2.177..." },
    { "key": "_fbp",      "value": "fb.2.177..." },
    { "key": "_ga",       "value": "GA1.1.197..." },
    { "key": "_nx_user",  "value": "nxu_kdkl543813h7" },
    { "key": "_ttp",      "value": "AiEUJV..." },
    { "key": "cart_id",   "value": "609235447" },
    { "key": "fbclid",    "value": "PAZXh0bg..." },
    { "key": "platform",  "value": "shopify" },
    { "key": "platform_order_number", "value": "35526" },
    { "key": "source_platform", "value": "shopify" }
  ]
}
```

### Conversão metadata → map

```typescript
function parseMetadata(data: YampiMetadataItem[]): Record<string, string> {
  return Object.fromEntries(data.map(({ key, value }) => [key, value]))
}

// Uso:
const meta = parseMetadata(resource.metadata.data)
const nx_user = meta['_nx_user']
const fbc     = meta['_fbc']
const fbp     = meta['_fbp']
const ttp     = meta['_ttp']
const fbclid  = meta['fbclid']
const ga      = meta['_ga']
```

### UTMs no root do resource

```typescript
const utm_source   = resource.utm_source    // "meta"
const utm_campaign = resource.utm_campaign  // "MAR_011_TST..."
const utm_medium   = resource.utm_medium    // "paid_social"
const utm_content  = resource.utm_content
const utm_term     = resource.utm_term
```

### Shopify integration check

```typescript
// resource.services.data[0]?.shopifyapp?.external_id = "7490301034792"
const shopifyOrderId = resource.services?.data?.[0]?.shopifyapp?.external_id
const isShopifyIntegrated = meta['source_platform'] === 'shopify' || !!shopifyOrderId
```

### Telefone E.164

O payload Yampi traz `phone.full_number: "71991487691"`. Normalizar para E.164:
```typescript
const phone = `+55${customer.phone.full_number}` // para BR
```

Usar `shared/phone-normalizer.ts` existente no worker.

### HMAC Yampi

Header: `x-yampi-hmac-sha256`
Secret: `merchant_token` configurado no projeto

```typescript
async function validateYampiHmac(body: string, signature: string, secret: string): Promise<boolean> {
  const key = await crypto.subtle.importKey(
    'raw', new TextEncoder().encode(secret), { name: 'HMAC', hash: 'SHA-256' }, false, ['verify']
  )
  const sig = Uint8Array.from(atob(signature), c => c.charCodeAt(0))
  return crypto.subtle.verify('HMAC', key, sig, new TextEncoder().encode(body))
}
```

---

## File List

Arquivos a criar:
- `worker/src/lib/gateways/yampi.parser.ts`

Arquivos a verificar/atualizar:
- `worker/src/handlers/gateways/yampi.ts` — usar novo parser

---

## 🤖 CodeRabbit Integration

**Story Type:** API + Integration
**Complexity:** Medium

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): Teste com payload real do webhook
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- `_nx_user` de metadata tem prioridade sobre matching por email (garantir no processor)
- Telefone normalizado para E.164 antes do hashing
- HMAC validation deve usar comparação em tempo constante

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-08 | @sm (River) | Story criada — Draft |
| 2026-05-08 | @po (Pax) | Validada GO (8/10) — Complexity adicionada → Status: Ready |
| 2026-05-08 | @dev (Dex) | Tasks 1-8,10 implementadas — `yampi.parser.ts`: parseMetadata, extractItems (array), extractCustomer E.164, validateHmac constant-time, `_nx_user` metadata priority → Status: Done |
