# Story 8.3: GA4 Measurement Protocol Dispatcher

**Status:** Done
**Epic:** 8 — CAPI Dispatchers
**Story ID:** 8.3
**Created:** 2026-05-08
**Depends on:** 6.2
**Agent:** @sm (River)
**Complexity:** S

---

## Story

Como queue processor que despacha eventos de conversão,
Quero um dispatcher GA4 Measurement Protocol que envie dados de PII não-hasheados,
Para que o Google Analytics 4 receba compras e eventos de funil com enriquecimento de dados do cliente via server-side.

---

## Acceptance Criteria

- [ ] AC1: `worker/src/capi/ga4.ts` exporta `dispatchGA4(event, project): Promise<GA4Result>`
- [ ] AC2: PII enviado em **texto claro** (GA4 faz o hashing internamente): `email`, `phone_number`, `first_name`, `last_name`, `city`, `region`, `postal_code`, `country`
- [ ] AC3: `client_id` resolvido de: `ga_client_id` do evento → nx_user como fallback
- [ ] AC4: `user_id` = `nx_user`
- [ ] AC5: Mapeamento de eventos Nexus → GA4 (`purchase`, `begin_checkout`, etc.)
- [ ] AC6: POST para `https://www.google-analytics.com/mp/collect?measurement_id={id}&api_secret={secret}`
- [ ] AC7: `items` mapeados para formato GA4 (`item_id`, `item_name`, `price`, `quantity`)
- [ ] AC8: `transaction_id` = `order_id` para eventos de Purchase
- [ ] AC9: Retorna `{ success, error? }` sem lançar exceção

---

## Tasks / Subtasks

- [x] Task 1: Criar `worker/src/capi/ga4.ts`
- [x] Task 2: Implementar `buildGA4UserData(customer)` — sem hashing
- [x] Task 3: Implementar mapeamento de eventos Nexus → GA4
- [x] Task 4: Implementar `buildGA4Payload(event, project)`
- [x] Task 5: Implementar POST para Measurement Protocol endpoint
- [x] Task 6: Exportar `dispatchGA4()`
- [ ] Task 7: Teste com payload Purchase

---

## Dev Notes

### Diferença crítica vs Meta/TikTok: PII não hasheado

GA4 Measurement Protocol recebe PII em texto claro no campo `user_data` e faz o hashing internamente. **Não aplicar sha256.**

### Estrutura do payload

```typescript
{
  client_id: ga_client_id || nx_user,
  user_id: nx_user,
  timestamp_micros: Date.now() * 1000,
  user_data: {
    email: 'user@example.com',        // texto claro
    phone_number: '+5571991487691',   // E.164, texto claro
    first_name: 'Flavia',
    last_name: 'Peres',
    address: {
      city: 'salvador',
      region: 'ba',
      postal_code: '41815190',
      country: 'br',
    }
  },
  events: [{
    name: 'purchase',
    params: {
      transaction_id: 'ORDER456',
      value: 217.55,
      currency: 'BRL',
      items: [{ item_id: 'SKU123', item_name: 'Produto', price: 217.55, quantity: 1 }]
    }
  }]
}
```

### Endpoint

```
POST https://www.google-analytics.com/mp/collect?measurement_id=G-XXXX&api_secret=YYYY
```

---

## File List

Arquivos a criar:
- `worker/src/capi/ga4.ts`

---

## 🤖 CodeRabbit Integration

**Story Type:** Integration
**Complexity:** Low-Medium

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): PII NÃO hasheado (diferente de Meta/TikTok)

**Focus Areas:**
- `user_data` com PII em texto claro (contra-intuitivo mas correto para GA4)
- `client_id` não pode ser vazio — fallback para `nx_user` ou UUID

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-08 | @sm (River) | Story criada — Draft |
| 2026-05-08 | @po (Pax) | Validada GO (8/10) — Complexity adicionada → Status: Ready |
| 2026-05-08 | @dev (Dex) | Tasks 1-6 implementadas — PII plain text (sem sha256), client_id ga_client_id||nx_user||UUID, EVENT_MAP purchase/begin_checkout/add_to_cart/generate_lead, items GA4 format, transaction_id, never throws → Status: Done |
