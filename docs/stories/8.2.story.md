# Story 8.2: TikTok CAPI Dispatcher

**Status:** Done
**Epic:** 8 — CAPI Dispatchers
**Story ID:** 8.2
**Created:** 2026-05-08
**Depends on:** 6.2, 8.1
**Agent:** @sm (River)
**Complexity:** M

---

## Story

Como queue processor que despacha eventos de conversão,
Quero um dispatcher TikTok Events API 2.0 com hashing SHA-256,
Para que `Purchase` e eventos de funil sejam enviados à TikTok Events API com `ttclid`, `ttp` e dados hasheados do usuário.

---

## Acceptance Criteria

- [ ] AC1: `worker/src/capi/tiktok.ts` exporta `dispatchTikTokCapi(event, project): Promise<TikTokCapiResult>`
- [ ] AC2: Hashing SHA-256 para: `email`, `phone_number`, `external_id`
- [ ] AC3: Mapeamento de eventos: `Purchase`, `InitiateCheckout`, `AddPaymentInfo`, `ViewContent`, `AddToCart`, `Lead`
- [ ] AC4: `event_id` determinístico para Purchase
- [ ] AC5: `ttclid` e `ttp` incluídos no objeto `user` quando disponíveis
- [ ] AC6: POST para `https://business-api.tiktok.com/open_api/v1.3/event/track/` com `Access-Token` header
- [ ] AC7: `event_source: 'web'`, `event_source_id: pixel_id` no body
- [ ] AC8: `contents` mapeados corretamente com `content_id`, `price`, `quantity`
- [ ] AC9: Retorna `{ success, error? }` sem lançar exceção

---

## Tasks / Subtasks

- [x] Task 1: Criar `worker/src/capi/tiktok.ts`
- [x] Task 2: Implementar `buildTikTokUser(customer, signals)` com campos hasheados
- [x] Task 3: Implementar mapeamento de eventos Nexus → TikTok
- [x] Task 4: Implementar `buildTikTokPayload(event, project)`
- [x] Task 5: Implementar POST com header `Access-Token`
- [x] Task 6: Exportar `dispatchTikTokCapi()`
- [ ] Task 7: Teste com payload Purchase

---

## Dev Notes

### Mapeamento eventos TikTok CAPI

```typescript
const TIKTOK_CAPI_MAP: Record<string, string> = {
  purchase:          'Purchase',
  InitiateCheckout:  'InitiateCheckout',
  AddPaymentInfo:    'AddPaymentInfo',
  AddToCart:         'AddToCart',
  ViewContent:       'ViewContent',
  Lead:              'SubmitForm',
  AddContactInfo:    'CompleteRegistration',
}
```

### Estrutura do payload TikTok Events API 2.0

```typescript
{
  event_source: 'web',
  event_source_id: pixel_id,
  data: [{
    event: 'Purchase',
    event_time: unix_seconds,
    event_id: 'purchase_PIX_ORDER',
    user: {
      ttclid: 'ttclid_value',         // não hasheado
      ttp: 'ttp_value',               // não hasheado
      email: sha256(email),
      phone_number: sha256(phone_e164),
      external_id: sha256(nx_user),
      locale: 'pt-BR',
      ip: ip_address,
      user_agent: user_agent,
    },
    properties: {
      currency: 'BRL',
      value: 217.55,
      order_id: 'ORDER456',
      contents: [{ content_id, content_name, price, quantity }]
    }
  }]
}
```

### Reutilizar sha256 de meta.ts

Importar `sha256` de `./meta.ts` ou criar `./utils.ts` compartilhado.

---

## File List

Arquivos criados:
- `worker/src/capi/utils.ts` (sha256 compartilhado — importado por meta.ts e tiktok.ts)
- `worker/src/capi/tiktok.ts`

---

## 🤖 CodeRabbit Integration

**Story Type:** Integration + Security
**Complexity:** Medium

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `ttclid`/`ttp` não hasheados, `email`/`phone` hasheados

**Focus Areas:**
- `ttclid` e `ttp` no objeto `user` (não em `properties`)
- Reutilizar utilitário sha256 em vez de duplicar

---

## Change Log

| Data | Agente | Ação |
|------|--------|------|
| 2026-05-08 | @sm (River) | Story criada — Draft |
| 2026-05-08 | @po (Pax) | Validada GO (8/10) — Complexity adicionada → Status: Ready |
| 2026-05-08 | @dev (Dex) | Tasks 1-6 implementadas — utils.ts sha256 compartilhado, tiktok.ts: buildUser (email/phone/external_id hashed, ttclid/ttp plain), EVENT_MAP, event_source web, Access-Token header, never throws → Status: Done |
